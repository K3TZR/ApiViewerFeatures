import XCTest
@testable import ApiViewerFeatures

final class ApiViewerFeaturesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ApiViewerFeatures().text, "Hello, World!")
    }
}
